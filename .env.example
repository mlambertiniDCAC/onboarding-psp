# Base URL de apigateway-psp (scheme + host; NO incluye /v1).
# Local: http://localhost:8010. Producción: se carga en Vercel, no se commitea.
# Sin prefijo VITE_: se inyecta al bundle explícitamente vía `define` en vite.config.js.
APIGW_PSP_URL=
