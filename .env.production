# Producción. El pipeline de deploy (Vercel) DEBE completar esta URL antes de buildear —
# vite la hornea en tiempo de build. Un build de prod con esto vacío FALLA (guard en vite.config.js).
# Apunta a la URL pública del túnel (ngrok/cloudflared) que expone el bridge local hacia apigateway-psp.
VITE_APIGW_PSP_URL=
